import { createStyles } from 'antd-style';

const useDiagnosisStyles = createStyles(() => ({
  diagnosisContainer: {
    background: '#fff',
    minWidth: 860,
    maxWidth: 1200,
    margin: '0 auto',
    padding: 24,
  },
}));

export default useDiagnosisStyles;
