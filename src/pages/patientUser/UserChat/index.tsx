import { PageContainer } from '@ant-design/pro-components';
import React from 'react';
import ChatPanel from '../UserList/components/ChatPanel';

const UserChat: React.FC = () => {
  return (
    <PageContainer>
      <ChatPanel />
    </PageContainer>
  );
};

export default UserChat;
